[package]
name = "memorydle"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
rocket = "0.5.1"
sqlx = {version = "0.8", features = ["runtime-tokio", "sqlite", "macros", "derive", "migrate"]}
dotenv = "0.15.0"